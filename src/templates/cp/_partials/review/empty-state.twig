{#
    Empty-state placeholder for the review grid when no transform cards are renderable.

    Variables:
      - message (string) Copy to display. Defaults to a standard no-results message.
#}
<div class="bpts-empty-state light">{{ message ?? 'Clean slate! Get processing.'|t('breakpoints') }}</div>
