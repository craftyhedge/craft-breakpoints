{% import '_includes/forms.twig' as forms %}

<div class="readable">
    <p>{{ 'Plugin settings are ready, but no configurable fields have been added yet.'|t('craft-breakpoint-images') }}</p>
</div>

{{ forms.field({
    label: 'Utility'|t('craft-breakpoint-images'),
    instructions: 'Open the utility while settings fields are being implemented.'|t('craft-breakpoint-images')
}, '<a class="go" href="' ~ cpUrl('utilities/breakpoint-images') ~ '">' ~ 'Go to utility'|t('craft-breakpoint-images') ~ '</a>') }}
